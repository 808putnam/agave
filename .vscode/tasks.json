{
    "version": "2.0.0",
    // Ctrl-Shift-B to run a task
    "tasks": [
        {
            "label": "node-setup",
            "type": "shell",
            "command": "./multinode-demo/setup.sh && ./multinode-demo/faucet.sh &",
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },
    ]
}